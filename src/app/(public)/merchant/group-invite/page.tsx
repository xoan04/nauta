import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Invitación a grupo · Perlapp",
};

export default function MerchantGroupInvitePage() {
  return <main className="min-h-screen bg-perlapp-canvas" />;
}
